language: cpp
sudo: required
dist: xenial

before_script:
  - sudo apt-get update
  - sudo apt-get install -y cmake

script:
  - cmake -H. -B_build -DBUILD_TESTS=ON
  - cmake --build _build
  - cmake --build _build --target test
