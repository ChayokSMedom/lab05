language: cpp
sudo: required
dist: focal

before_script:
  - sudo apt-get update
  - sudo apt-get install -y cmake

script:
  - cmake -H. -B_build -DBUILD_TESTS=ON
  - cmake --build _build
  - cd _build
  - ctest --output-on-failure
